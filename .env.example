# ==========================
# Flask API environment config
# ==========================

# Your OpenAI secret key (starts with "sk-" or "sk-proj-...")
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional: specify which OpenAI model to use
OPENAI_CHAT_MODEL=gpt-4o-mini

# CORS policy — restrict who can call your API (or use * to allow all)
ALLOWED_ORIGIN=https://yourwordpresssite.com

# For local dev only (Render sets this automatically)
PORT=5000
